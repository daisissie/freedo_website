{
  "name": "freedo-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:edgeone": "npm run build && rm -rf dist/node-functions && cp -R node-functions dist/node-functions && cp package.json dist/package.json",
    "preview": "vite preview",
    "smoke:demo": "node scripts/smoke-demo.mjs"
  },
  "dependencies": {
    "@google/model-viewer": "^4.1.0",
    "three": "^0.181.1"
  },
  "devDependencies": {
    "vite": "^7.2.1"
  }
}
